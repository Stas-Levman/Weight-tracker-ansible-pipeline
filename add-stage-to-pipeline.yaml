---
- hosts: localhost

  roles:
    - role: get-vmss-inventory
      vars:
        resource_group_name: "weight-tracker-{{ env }}-rg"


- hosts: scalesethosts
  become: true

  roles:
    - role: add-to-azure-devops
      vars:
        env: "{{ env }}"
